<div class="row">
    <div class="col-sm-12 col-md-10">
        <div class="d-flex justify-content-between align-items-center mb-3 pb-2 border-bottom">
            <h2>ایجاد دسته بهداشت شخصی</h2>
        </div>
    </div>
    <div class="col-sm-12 col-md-2">
        <div class="text-left">
            <a href="/admin/categories_health" type="submit" class="btn btn-info">بازگشت</a>
        </div>
    </div>
</div><!--row-->

<form class="form-horizontal" action="/admin/categories_health/create" method="post">

    <%- include(viewPath('layouts/error-messages')) -%>
    <div class="form-group row">
        <div class="col">
            <label for="name" class="control-label font-weight-bold">عنوان دسته</label>
            <input type="text" class="form-control" name="name" id="name" placeholder="عنوان را وارد کنید" value="<%= old('name') %>">
        </div>
    </div>
    
    <div class="form-group row">
        <div class="col">
            <label for="parent" class="control-label font-weight-bold font-weight-bold">دسته مربوطه</label>
            <select name="parent" id="parent" class="form-control">
                    <option value="none">دسته اصلی</option>
                <% categories_health.forEach(cate_health => { %> 
                    <option value="<%= cate_health.id %>" <%= String(old('categories_health')) == String(cate_health.id) ? 'selected' : '' %> ><%= cate_health.name %></option>
                <% }) %>
            </select>
        </div>
    </div>

    <div class="form-group row">
        <div class="col">
            <button type="submit" class="btn btn-danger">ارسال</button>
        </div>
    </div>
</form>
