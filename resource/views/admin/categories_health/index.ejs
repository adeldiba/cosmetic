<div class="d-flex justify-content-between align-items-center mb-3 border-bottom-1">
        <h2>دسته های بهداشت شخصی</h2>
        <div class="btn-group">
            <a href="/admin/categories_health/create" class="btn btn-sm btn-primary">افزودن دسته جدید</a>
        </div>
    </div>
    <div class="table-responsive">
        <table class="table table-striped table-bordered">
            <thead>
            <tr>
                <th>عنوان دسته</th>
                <th>نوع</th>
                <th>تنظیمات</th>
            </tr>
            </thead>
            <tbody>
                <% categories_health.docs.forEach(category_health => { %>
                    <tr>
                        <td><a href="#"><%= category_health.name %></a></td>
                        <td><%= category_health.parent ? category_health.parent.name : 'دسته اصلی' %></td>
                        <td>
                            <form action="/admin/categories_health/<%= category_health.id %>?_method=DELETE" method="post">
                                
                                <div class=" btn-group-sm"> 
                                    <a href="/admin/categories_health/<%= category_health.id %>/edit"  class="btn btn-primary">ویرایش</a>
                                    <button type="submit" class="btn btn-danger">حذف</button>
                                </div>
                            </form>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>

        <%- include(viewPath('layouts/pagination') ,{ pagination : categories_health}) %>
    </div>
    
    