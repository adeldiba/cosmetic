<div class="d-flex justify-content-between align-items-center mb-3 border-bottom-1">
        <h2>گالری تصاویر</h2>
        <div class="btn-group">
            <a href="/admin/gullerys_skin/create" class="btn btn-sm btn-primary">ایجاد گالری تصاویر</a>
        </div>
    </div>
    
    <hr>
    <div class="table-responsive">
        <table class="table table-bordered">
            <thead>
            <tr>
                <th>محصول مربوطه</th>
                <th>تصویر محصول</th>
                <th>تاریخ</th>
                <th>تنظیمات</th>
            </tr>
            </thead>
            <tbody>
                <% gullerys_skin.docs.forEach(gullery_skin =>{ %>
                    <tr>
                        <td><%= gullery_skin.skin.title %></td>
                        <td><img src="/product_images/<%= gullery_skin.id %>/<%= gullery_skin.image %>" class="img-fluid" width="100px" alt=""></td>
                        <td>
                            <%= date(gullery_skin.createdAt).format('jD jMMMM jYYYY') %>
                        </td>
                        <td>
                            <form action="/admin/gullerys_skin/<%= gullery_skin._id %>?_method=DELETE" method="post">
                                
                                <input type="hidden" name="_method" value="delete">
                                <input type="hidden" name="_token" value="Dso45XzqoXsAkRC8BBMQ63ZsOFS0c2tELkxR4i3Y">
                                <div class="btn-group-sm">
                                   
                                    <button type="submit" class="btn btn-danger">حذف</button>
                                </div>
                            </form>
                        </td>
                    </tr>
                    <% }); %>
            </tbody>
        </table>
        
        <%- include(viewPath('layouts/pagination') ,{ pagination : gullerys_skin}) %>
    </div>
    
    