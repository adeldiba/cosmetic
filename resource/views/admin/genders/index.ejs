<div class="d-flex justify-content-between align-items-center mb-3 border-bottom-1">
        <h2>جنسیت :  مردانه یا زنانه</h2>
        <div class="btn-group">
            <a href="/admin/genders/create" class="btn btn-sm btn-primary">ایجاد جنسیت</a>
        </div>
    </div>
    <div class="table-responsive">
        <table class="table table-striped table-bordered">
            <thead>
            <tr>
                <th> جنسیت</th>
                <th>نوع</th>
                <th>تاریخ</th>
                <th>تنظیمات</th>
               
            </tr>
            </thead>
            <tbody>
                <% genders.docs.forEach(gender =>{ %>
                    <tr>
                        <td><%= gender.name %></td>
                        <td><%= gender.parent ? gender.parent.name : 'دسته اصلی' %></td>
                        <td><%= date(gender.createdAt).format('jD jMMMM jYYYY') %></td>
                        <td>
                            <form action="/admin/genders/<%= gender._id %>?_method=DELETE" method="post">
                                <input type="hidden" name="_method" value="delete">
                                <input type="hidden" name="_token" value="Dso45XzqoXsAkRC8BBMQ63ZsOFS0c2tELkxR4i3Y">
                                <div class="btn-group-sm">
                                    <a href="/admin/genders/<%= gender._id %>/edit"  class="btn btn-primary">ویرایش</a>
                                    <button type="submit" class="btn btn-danger">حذف</button>
                                </div>
                            </form>
                        </td>
                    </tr>
                    <% }); %>
            </tbody>
        </table>
        <%- include(viewPath('layouts/pagination'), {pagination:  genders}) %>
    </div>
    
    