<div class="d-flex justify-content-between align-items-center mb-3 border-bottom-1">
        <h2>محتواهای پاسخ به پرسش های متداول</h2>
        <div class="btn-group">
            <a href="/admin/response/create" class="btn btn-sm btn-primary">ایجاد محتوای پاسخ به پرسش های متداول</a>
        </div>
    </div>
    <div class="table-responsive">
        <table class="table table-striped table-bordered">
            <thead>
            <tr>
                <th> نام محتوا</th>
                <th>تاریخ</th>
                <th>تنظیمات</th>
               
            </tr>
            </thead>
            <tbody>
                <% response.forEach(response =>{ %>
                    <tr>
                        <td><%= response.title %></td>
                        <td><%= date(response.createdAt).format('jD jMMMM jYYYY') %></td>
                        <td>
                            <form action="/admin/response/<%= response._id %>?_method=DELETE" method="post">
                                <input type="hidden" name="_method" value="delete">
                                <input type="hidden" name="_token" value="Dso45XzqoXsAkRC8BBMQ63ZsOFS0c2tELkxR4i3Y">
                                <div class=" btn-group-sm">
                                    <a href="/admin/response/<%= response._id %>/edit"  class="btn btn-primary">ویرایش</a>
                                    <button type="submit" class="btn btn-danger">حذف</button>
                                </div>
                            </form>
                        </td>
                    </tr>
                    <% }); %>
            </tbody>
        </table>
    </div>
    
    